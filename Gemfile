source :rubygems

gem 'puppet'
gem 'puppet-lint'
gem 'rake'

group :development do
  # For local testing with VMs.
  gem 'vagrant'

  # Make testing easier in development.
  gem 'guard'
  gem 'growl'
  gem 'guard-puppet-lint'
end
