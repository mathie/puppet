source 'https://rubygems.org/'

# Stuck with an old version of nokogiri 'til I've updated the puppet stuff to 1.9.x.
gem 'nokogiri', '~>1.5.10'

gem 'puppet'
gem 'puppet-lint'
gem 'rake'

# For bootstrapping Rackspace Cloud nodes
gem 'fog'

group :development do
  # Make testing easier in development.
  gem 'guard'
  gem 'growl'
  gem 'rb-fsevent'
  gem 'guard-puppet-lint'
  gem 'guard-bundler'
end
